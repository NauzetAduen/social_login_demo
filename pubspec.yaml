name: social_login_demo
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  #firebase
  firebase_core: "0.5.0"
  firebase_auth:
  google_sign_in:
  font_awesome_flutter:
  provider:


  cupertino_icons: ^0.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  mockito:

flutter:


  uses-material-design: true
